import { describe, expect, it } from 'vitest'

// Placeholder that proves the Vitest toolchain runs. The real unit-test
// burden lands in M2 with the go-able filter (AGENTS.md "Testing
// expectations") — delete this file then.
describe('toolchain', () => {
  it('runs vitest', () => {
    expect(true).toBe(true)
  })
})
